# Frontend

The frontend will stay intentionally simple. Its main responsibilities are:

- Collect ticker, date range, capital, and strategy input
- Call the backend API
- Display metrics, equity curve, and trade records

Frontend implementation will start after the backend MVP flow is working.
